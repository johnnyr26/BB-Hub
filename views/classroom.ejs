<!DOCTYPE html>
<html>
    <head></head>
    <body>
        <% if (locals.assignments) { %>
            <% assignments.forEach(assignment => { %>
                <% const { name, title } = assignment; %>
                <p><%= name %>: <%= title %></p>
            <% }); %>
        <% } %>
    </body>
</html>